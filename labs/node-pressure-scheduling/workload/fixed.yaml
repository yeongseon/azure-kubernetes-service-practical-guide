apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-scheduling-lab
  namespace: workload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-scheduling-lab
  template:
    metadata:
      labels:
        app: node-scheduling-lab
    spec:
      containers:
        - name: app
          image: "${LAB_IMAGE}"
          ports:
            - containerPort: 8000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
